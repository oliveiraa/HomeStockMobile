%h2 Estoque
= form_for(@stock) do |f|
	%fieldset(data-role="fieldcontain")
		= f.label :name
		= f.text_field :name
	%fieldset(data-role="fieldcontain")
		= f.label :description
		= f.text_area :description
	%input(type="submit" value="Enviar")